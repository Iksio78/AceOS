[package]
name = "aceos"
version = "0.1.0"
edition = "2021"
test = false
bench = false
harness = false

[dependencies]
limine = "0.6.5"
spin = "0.9.9"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
